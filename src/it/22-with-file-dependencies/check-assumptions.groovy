// The integration test exists in Gradle Plugin to test dependencies that are available as plain files
// and are not available in Maven Central. Maven does not have such a feature, so the test does not exist here.
// We added it as always skip to synchronize integration tests numbering with Gradle plugin.
return false